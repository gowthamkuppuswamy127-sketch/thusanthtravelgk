---
import { Image } from 'astro:assets';
import Phone from '@lucide/astro/icons/phone';
import Menu from '@lucide/astro/icons/menu';
import X from '@lucide/astro/icons/x';
import ArrowRight from '@lucide/astro/icons/arrow-right';
import logo from '../assets/brand/logo-on-dark.png';
import WhatsAppIcon from './WhatsAppIcon.astro';
import { nav, site, telHref, whatsappHref } from '../data/site';

const path = Astro.url.pathname.replace(/\/$/, '') || '/';
const isActive = (href: string) => (href === '/' ? path === '/' : path.startsWith(href));
---

<header class="site-header fixed inset-x-0 top-0 z-50 px-3 pt-3 sm:px-4 sm:pt-4" data-header>
  <div class="mx-auto max-w-7xl">
    <nav
      class="nav-pill flex items-center justify-between gap-4 rounded-2xl py-1.5 pr-2 pl-3 sm:pl-4 lg:rounded-full lg:pl-6"
      aria-label="Main"
    >
      <a href="/" class="flex shrink-0 items-center" aria-label={`${site.name}, home`}>
        <Image src={logo} alt="" height={56} densities={[1, 2]} class="h-11 w-auto sm:h-[52px]" loading="eager" />
      </a>

      <ul class="hidden items-center gap-1 lg:flex">
        {
          nav.map((item) => (
            <li>
              <a
                href={item.href}
                class:list={['nav-link', isActive(item.href) && 'is-active']}
                aria-current={isActive(item.href) ? 'page' : undefined}
              >
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>

      <div class="flex items-center gap-2">
        <a
          href={telHref}
          class="hidden items-center gap-2 rounded-full px-3 py-2 text-sm font-medium text-sand-100/90 transition hover:text-gold-200 xl:inline-flex"
        >
          <Phone size={16} />
          {site.phoneDisplay}
        </a>
        <a href="/contact#enquiry" class="btn btn-gold btn-sm hidden sm:inline-flex" data-enquire>
          Book now
        </a>
        <button
          type="button"
          class="grid size-11 place-items-center rounded-full text-sand-100 transition hover:bg-white/10 lg:hidden"
          aria-expanded="false"
          aria-controls="mobile-menu"
          data-menu-toggle
        >
          <span class="sr-only">Open menu</span>
          <Menu size={22} class="icon-open" />
          <X size={22} class="icon-close hidden" />
        </button>
      </div>
    </nav>

    <div id="mobile-menu" class="glass-strong mt-2 rounded-2xl p-3 lg:hidden" hidden data-mobile-menu>
      <ul class="grid gap-1">
        {
          nav.map((item) => (
            <li>
              <a
                href={item.href}
                class:list={[
                  'flex items-center justify-between rounded-xl px-4 py-3.5 text-lg font-medium transition hover:bg-white/8',
                  isActive(item.href) ? 'text-gold-200' : 'text-sand-100',
                ]}
                aria-current={isActive(item.href) ? 'page' : undefined}
              >
                {item.label}
                <ArrowRight size={18} class="opacity-50" />
              </a>
            </li>
          ))
        }
      </ul>
      <div class="mt-3 grid grid-cols-2 gap-2 border-t border-white/10 pt-3">
        <a href={telHref} class="btn btn-glass">
          <Phone size={17} /> Call
        </a>
        <a href={whatsappHref()} class="btn btn-glass" target="_blank" rel="noopener">
          <WhatsAppIcon size={17} /> WhatsApp
        </a>
        <a href="/contact#enquiry" class="btn btn-gold col-span-2" data-enquire>Book now</a>
      </div>
    </div>
  </div>
</header>

<style>
  .nav-pill {
    background: rgb(4 28 30 / 0.28);
    border: 1px solid rgb(255 255 255 / 0.12);
    -webkit-backdrop-filter: blur(16px) saturate(150%);
    backdrop-filter: blur(16px) saturate(150%);
    transition:
      background 0.35s ease,
      box-shadow 0.35s ease;
  }
  [data-scrolled] .nav-pill,
  [data-menu-open] .nav-pill {
    background: rgb(4 28 30 / 0.8);
    box-shadow: 0 18px 40px -24px rgb(0 0 0 / 0.8);
  }
  @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
    .nav-pill {
      background: rgb(4 28 30 / 0.92);
    }
  }
  .nav-link {
    position: relative;
    display: inline-block;
    padding: 0.55rem 0.95rem;
    border-radius: 999px;
    font-size: 0.92rem;
    font-weight: 500;
    color: rgb(246 239 226 / 0.82);
    transition:
      color 0.2s ease,
      background 0.2s ease;
  }
  .nav-link:hover {
    color: var(--color-sand-50);
    background: rgb(255 255 255 / 0.07);
  }
  .nav-link.is-active {
    color: var(--color-gold-200);
  }
  .nav-link.is-active::after {
    content: '';
    position: absolute;
    left: 50%;
    bottom: 0.2rem;
    width: 1.1rem;
    height: 2px;
    border-radius: 2px;
    background: var(--color-gold-300);
    transform: translateX(-50%);
  }
  [data-menu-open] :global(.icon-open) {
    display: none;
  }
  [data-menu-open] :global(.icon-close) {
    display: block;
  }
</style>
